# Python
__pycache__/
*.py[cod]
*$py.class
.Python
.pytest_cache/
.coverage
htmlcov/

# Venv
venv/
.venv/
env/

# AWS / secrets — NEVER commit these
.aws/
*.pem
*.key
.env

# IDE
.vscode/
.idea/
*.iml

# macOS
.DS_Store

# Office lock files
~$*

# Build artifacts
*.zip
*.tar.gz
dist/
build/

# Synthetic data outputs (you generate fresh per session)
data/generated/

# Local trainer scratch
scratch/

# SAM
.aws-sam/

# Note: src/common-layer/python/common/ IS committed to the repo. It's a
# copy of src/common/ that SAM packages as the AccelyaCommonLayer Lambda
# Layer. Trainer responsibility: re-sync via the deploy script's pre-build
# step (cp -r src/common/* src/common-layer/python/common/).
